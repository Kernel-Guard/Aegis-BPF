name: Go-Live Gate

on:
  workflow_dispatch:

jobs:
  readiness:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang llvm libbpf-dev libsystemd-dev pkg-config cmake ninja-build rpm python3-jsonschema linux-tools-common linux-tools-$(uname -r) || sudo apt-get install -y clang llvm libbpf-dev libsystemd-dev pkg-config cmake ninja-build rpm python3-jsonschema linux-tools-common

      - name: Run release-readiness gate
        run: |
          mkdir -p artifacts/go-live/readiness
          set -o pipefail
          scripts/release_readiness.sh | tee artifacts/go-live/readiness/release_readiness.log

      - name: Run key-rotation drill
        run: |
          mkdir -p artifacts/go-live/readiness
          set -o pipefail
          BUILD_DIR=build-key-drill scripts/key_rotation_drill.sh | tee artifacts/go-live/readiness/key_rotation_drill.log

      - name: Upload readiness artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: go-live-readiness
          path: artifacts/go-live/readiness/
          if-no-files-found: warn

  runtime-canary:
    runs-on: [self-hosted, bpf-lsm]
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v6

      - name: Install dependencies (if apt-get is available)
        run: |
          if command -v apt-get >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y clang llvm libbpf-dev libsystemd-dev pkg-config cmake ninja-build python3-jsonschema
            if ! command -v bpftool >/dev/null 2>&1; then
              sudo apt-get install -y linux-tools-common || true
              sudo apt-get install -y "linux-tools-$(uname -r)" || \
                sudo apt-get install -y linux-tools-generic || \
                sudo apt-get install -y linux-tools-gcp || true
            fi
            if ! command -v bpftool >/dev/null 2>&1; then
              echo "bpftool is required but was not installed" >&2
              exit 1
            fi
          fi

      - name: Verify environment
        run: scripts/verify_env.sh --strict

      - name: Require BPF LSM
        run: grep -qw bpf /sys/kernel/security/lsm

      - name: Configure
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=ON

      - name: Build
        run: cmake --build build

      - name: Unit tests
        run: ctest --test-dir build --output-on-failure --timeout 180

      - name: Canary gate (audit)
        run: sudo AEGIS_BIN=./build/aegisbpf PHASE=audit DURATION_SECONDS=180 MAX_RINGBUF_DROPS=100 MAX_RSS_GROWTH_KB=65536 MAX_EVENT_DROP_RATIO_PCT=0.1 MIN_TOTAL_DECISIONS=100 scripts/canary_gate.sh

      - name: Canary gate (enforce)
        run: sudo AEGIS_BIN=./build/aegisbpf PHASE=enforce ENFORCE_SIGNAL=term DURATION_SECONDS=180 MAX_RINGBUF_DROPS=100 MAX_RSS_GROWTH_KB=65536 MAX_EVENT_DROP_RATIO_PCT=0.1 MIN_TOTAL_DECISIONS=100 scripts/canary_gate.sh

      - name: Incident evidence bundle
        run: |
          mkdir -p artifacts/go-live
          sudo AEGIS_BIN=./build/aegisbpf scripts/collect_incident_bundle.sh artifacts/go-live/incident-gate

      - name: Upload runtime artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: go-live-runtime
          path: |
            artifacts/canary/
            artifacts/go-live/incident-gate.tar.gz
          if-no-files-found: warn

  perf-certification:
    runs-on: [self-hosted, perf]
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6

      - name: Install dependencies (if apt-get is available)
        run: |
          if command -v apt-get >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y clang llvm libbpf-dev libsystemd-dev pkg-config cmake ninja-build python3-jsonschema
            if ! command -v bpftool >/dev/null 2>&1; then
              sudo apt-get install -y linux-tools-common || true
              sudo apt-get install -y "linux-tools-$(uname -r)" || \
                sudo apt-get install -y linux-tools-generic || \
                sudo apt-get install -y linux-tools-gcp || true
            fi
            if ! command -v bpftool >/dev/null 2>&1; then
              echo "bpftool is required but was not installed" >&2
              exit 1
            fi
          fi

      - name: Verify environment
        run: scripts/verify_env.sh --strict

      - name: Configure
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF

      - name: Build
        run: cmake --build build

      - name: Set CPU governor (performance)
        run: |
          if [[ -r /sys/devices/system/cpu/cpu0/cpufreq/scaling_available_governors ]] && \
             grep -qw performance /sys/devices/system/cpu/cpu0/cpufreq/scaling_available_governors; then
            echo "Setting CPU governor=performance for perf gate (best-effort)"
            for g in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do
              echo performance | sudo tee "$g" >/dev/null || true
            done
            echo "cpu0 governor=$(cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor 2>/dev/null || echo unknown)"
          else
            echo "CPU governor control not available; continuing"
          fi

      - name: Perf microbench gate
        run: |
          mkdir -p artifacts/go-live/perf
          set -o pipefail
          sudo OUT_JSON=artifacts/go-live/perf/open_compare.json \
            MAX_PCT=10 ITERATIONS=200000 FILE=/etc/hosts scripts/perf_compare.sh \
            | tee artifacts/go-live/perf/perf_compare.log

      - name: Perf workload suite gate
        run: |
          mkdir -p artifacts/go-live/perf
          set -o pipefail
          sudo BIN=./build/aegisbpf \
            FILE=/etc/hosts \
            OPEN_ITERATIONS=200000 \
            READ_ITERATIONS=50000 \
            STAT_SAMPLE=400 \
            STAT_ITERATIONS=50 \
            MAX_OPEN_PCT=10 \
            MAX_READ_PCT=15 \
            MAX_STAT_PCT=15 \
            FORMAT=json \
            OUT=artifacts/go-live/perf/workload_suite.json \
            scripts/perf_workload_suite.sh | tee artifacts/go-live/perf/perf_workload.log

      - name: Capture percentile profiles (open/connect)
        run: |
          mkdir -p artifacts/go-live/perf
          sudo FORMAT=json OUT=artifacts/go-live/perf/open_baseline.json ITERATIONS=100000 FILE=/etc/hosts scripts/perf_open_bench.sh
          sudo WITH_AGENT=1 BIN=./build/aegisbpf FORMAT=json OUT=artifacts/go-live/perf/open_with_agent.json ITERATIONS=100000 FILE=/etc/hosts scripts/perf_open_bench.sh
          sudo FORMAT=json OUT=artifacts/go-live/perf/connect_baseline.json ITERATIONS=50000 scripts/perf_connect_bench.sh
          sudo WITH_AGENT=1 BIN=./build/aegisbpf FORMAT=json OUT=artifacts/go-live/perf/connect_with_agent.json ITERATIONS=50000 scripts/perf_connect_bench.sh

      - name: Perf SLO canonical gate
        run: |
          OPEN_JSON=artifacts/go-live/perf/open_compare.json \
          WORKLOAD_JSON=artifacts/go-live/perf/workload_suite.json \
          OPEN_BASELINE_JSON=artifacts/go-live/perf/open_baseline.json \
          OPEN_WITH_AGENT_JSON=artifacts/go-live/perf/open_with_agent.json \
          CONNECT_BASELINE_JSON=artifacts/go-live/perf/connect_baseline.json \
          CONNECT_WITH_AGENT_JSON=artifacts/go-live/perf/connect_with_agent.json \
          REPORT_OUT=artifacts/go-live/perf/perf-slo-report.md \
          SUMMARY_OUT=artifacts/go-live/perf/perf-slo-summary.json \
          MAX_OPEN_P95_OVERHEAD=10 \
          MAX_CONNECT_P95_OVERHEAD=10 \
          scripts/perf_slo_check.sh

      - name: Capture perf metadata
        run: |
          uname -a > artifacts/go-live/perf/kernel-info.txt
          cat /etc/os-release > artifacts/go-live/perf/os-release.txt || true
          lscpu > artifacts/go-live/perf/cpu-info.txt || true
          stat -f -c '%T' /tmp > artifacts/go-live/perf/fs-type.txt || true

      - name: Validate perf artifact schema and KPI ratios
        run: |
          python3 scripts/validate_perf_artifacts.py \
            --open-baseline artifacts/go-live/perf/open_baseline.json \
            --open-with-agent artifacts/go-live/perf/open_with_agent.json \
            --connect-baseline artifacts/go-live/perf/connect_baseline.json \
            --connect-with-agent artifacts/go-live/perf/connect_with_agent.json \
            --workload artifacts/go-live/perf/workload_suite.json \
            --report artifacts/go-live/perf/perf-evidence-report.md \
            --max-open-p95-ratio 1.05 \
            --max-connect-p95-ratio 1.05

      - name: Upload perf artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: go-live-perf
          path: artifacts/go-live/perf/
          if-no-files-found: warn

  release-drill:
    runs-on: [self-hosted, bpf-lsm]
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v6

      - name: Install dependencies (if apt-get is available)
        run: |
          if command -v apt-get >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y clang llvm libbpf-dev libsystemd-dev pkg-config cmake ninja-build rpm python3-jsonschema linux-tools-common linux-tools-$(uname -r) || sudo apt-get install -y clang llvm libbpf-dev libsystemd-dev pkg-config cmake ninja-build rpm python3-jsonschema linux-tools-common
            if ! command -v bpftool >/dev/null 2>&1; then
              sudo apt-get install -y "linux-tools-$(uname -r)" || \
                sudo apt-get install -y linux-tools-generic || \
                sudo apt-get install -y linux-tools-gcp || true
            fi
            if ! command -v bpftool >/dev/null 2>&1; then
              echo "bpftool is required but was not installed" >&2
              exit 1
            fi
          fi

      - name: Verify environment
        run: scripts/verify_env.sh --strict

      - name: Require BPF LSM
        run: grep -qw bpf /sys/kernel/security/lsm

      - name: Run full release drill
        run: BUILD_DIR=build-release-drill SKIP_BPF_BUILD=OFF ARTIFACT_DIR=artifacts/go-live/release-drill scripts/release_drill.sh

      - name: Upload release-drill artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: go-live-release-drill
          path: artifacts/go-live/release-drill/
          if-no-files-found: warn

  aggregate-evidence:
    if: always()
    needs: [readiness, runtime-canary, perf-certification, release-drill]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: go-live-evidence

      - name: Bundle go-live evidence
        run: |
          mkdir -p bundle
          find go-live-evidence -type f | sort > bundle/evidence_manifest.txt || true
          cp -r go-live-evidence bundle/evidence
          tar -czf go-live-evidence-${GITHUB_RUN_ID}.tar.gz -C bundle .

      - name: Upload aggregated evidence bundle
        uses: actions/upload-artifact@v4
        with:
          name: go-live-evidence-bundle
          path: |
            go-live-evidence-*.tar.gz
            bundle/evidence_manifest.txt
